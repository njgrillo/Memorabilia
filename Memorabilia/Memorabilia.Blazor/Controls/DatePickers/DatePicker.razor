<MudDatePicker Date="@SelectedDate" 
               DateChanged="@((p) => SelectedDateChanged.InvokeAsync(p))"
               DateFormat="@DateFormat"
               Editable="@Editable"
               Label="@Label"
               Mask="@Mask"
               Variant="@Variant" />
