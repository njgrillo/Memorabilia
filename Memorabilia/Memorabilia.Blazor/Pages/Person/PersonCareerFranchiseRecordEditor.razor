<DivRowFormGroup>
    <RowStack Class="@DivClass.Column12"
              Style="@Style.MarginBottomPad1">
        <RecordTypeAutoComplete Disabled="@(EditMode == EditModeType.Update)"
                                ResetValueOnEmptyText="false"
                                SelectedValue="Model.RecordType"
                                SelectionChanged="(p) => Model.RecordType = p"
                                Sports="Sports" />
        <FranchiseAutoComplete Franchises="Franchises" 
                               SelectedValue="Model.Franchise"
                               SelectionChanged="(p) => Model.Franchise = p"
                               Sports="Sports" />
        <TextField Label="Record"
                   SelectedText="@Model.Record"
                   SelectedTextChanged="@((p) => Model.Record = p)" />
        <AddUpdateFab EditMode="EditMode"
                      OnAdd="Add"
                      OnUpdate="Update" />
    </RowStack>
</DivRowFormGroup>
<Div Class="@DivClass.Row">
    <Div Class="@DivClass.Column12">
        <MudTable Bordered="false"
                  Dense="false"
                  Filter="new Func<PersonCareerFranchiseRecordEditModel, bool>(Filter)"
                  Hover="true"
                  Items="CareerFranchiseRecords.ActiveItems().OrderBy(record => record.RecordTypeName).ToList()"
                  Striped="true"
                  Virtualize="true">
            <NoRecordsContent>
                <NoRecords Text="No records found" />
            </NoRecordsContent>
            <ToolBarContent>
                <ToolbarTitle Text="Career Franchise Records" />
                <MudSpacer />
                <ToolbarSearchField SearchText="@_search"
                                    SearchTextChanged="@((p) => _search = p)" />
            </ToolBarContent>
            <HeaderContent>
                <SortedColumn SortBy="new Func<PersonCareerFranchiseRecordEditModel, object>(x => x.RecordTypeName)" Title="Record Type" />
                <SortedColumn SortBy="new Func<PersonCareerFranchiseRecordEditModel, object>(x => x.Franchise?.Name)" Title="Franchise" />
                <SortedColumn SortBy="new Func<PersonCareerFranchiseRecordEditModel, object>(x => x.Record)" Title="Record" />
                <MudTh></MudTh>
            </HeaderContent>
            <RowTemplate>
                <MudTd DataLabel="RecordType">@context.RecordTypeName</MudTd>
                <MudTd DataLabel="Franchise">@context.Franchise?.Name</MudTd>
                <MudTd DataLabel="Record">@context.Record</MudTd>
                <TableDataRightAligned>
                    <EditGridButton OnEdit="@(_ => Edit(context))" />
                    <DeleteGridButton OnDelete="@(_ => context.Delete())" />
                </TableDataRightAligned>
            </RowTemplate>
            <PagerContent>
                <MudTablePager />
            </PagerContent>
        </MudTable>
    </Div>
</Div>