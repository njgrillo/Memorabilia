<DivRowFormGroup Class="@Class">
    <RowStack Class="@DivClass.Column12"
              Style="@Style.MarginBottomPad1">
        <PersonSearchAutoComplete IsCulturalSearch="true"
                                  SelectedValue="SelectedPerson"
                                  SelectionChanged="(p) => SelectedPerson = p" />
        <AddFab OnAdd="Add" />
    </RowStack>       
</DivRowFormGroup>
<DivRowCol12 Style="@Style.MarginTopPad1">
    <MudTable Bordered="false"
              Dense="false"
              Hover="true"
              Items="SelectedPeople.ToList()"
              Striped="true">
        <NoRecordsContent>
            <NoRecords Text="No people selected" />
        </NoRecordsContent>
        <HeaderContent>
            <MudTh>Selected People</MudTh>
            <MudTh></MudTh>
            <MudTh></MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd DataLabel="Person">@context.DisplayName</MudTd>
            <MudTd>
                <Fab Color="MudBlazor.Color.Secondary"
                        OnClick="@(_ => ShowPersonProfile(context.Id))"
                        StartIcon="@Icons.Material.Filled.Person"
                        TooltipText="View Profile" />
            </MudTd>
            <TableDataRightAligned>
                <DeleteGridButton OnDelete="@(_ => RemovePerson(context))" />
            </TableDataRightAligned>
        </RowTemplate>
    </MudTable>
</DivRowCol12>
