<Div Class="row">
    <Div Class="col-3">
        <SportLeagueLevelDropDown Disabled="@(!_canEditSportLeagueLevel)" 
                                  SelectedValue="Model.SportLeagueLevelId"
                                  SelectionChanged="(p) => Model.SportLeagueLevelId = p" />
    </Div> 
    <Div Class="col-3">
        <BallotNumberDropDown SelectedValue="Model.BallotNumber"
                              SelectionChanged="(p) => Model.BallotNumber = p" />
    </Div>   
    <Div Class="col-3">
        <NullableIntField Label="Induction Year"
                          Value="Model.InductionYear"
                          ValueChanged="@((p) => Model.InductionYear = p)" />
    </Div>
    <Div Class="col-3">
        <NullableDecimalField Label="Vote Percentage"
                              Maximum="100"
                              Value="Model.VotePercentage"
                              ValueChanged="@((p) => Model.VotePercentage = p)" />
    </Div>
</Div>
<br />
<DivLeftAlign>
    <Div Hidden="@(!_canAddHallOfFame)">
        <AddButton OnClick="Add" />
    </Div>
    <Div Hidden="@(!_canUpdateHallOfFame)">
        <UpdateButton OnClick="Update" />
    </Div>
</DivLeftAlign>
<br />
<Div Class="row">
    <Div Class="col-6">
         <MudTable Bordered="false"
                   Dense="false" 
                   Hover="true"
                   Items="@HallOfFames.Where(hof => !hof.IsDeleted).ToList()"    
                   Striped="true">
            <HeaderContent>
                <MudTh>Sport</MudTh>
                <MudTh>Induction Year</MudTh>
                <MudTh>Vote Percentage</MudTh>
                <MudTh>Ballot</MudTh>
                <MudTh></MudTh>
            </HeaderContent>
            <RowTemplate>
                <MudTd DataLabel="Sport">@context.SportName</MudTd>
                <MudTd DataLabel="Induction Year">@context.InductionYear</MudTd>
                <MudTd DataLabel="Vote Percentage">@context.VotePercentage</MudTd>
                <MudTd DataLabel="Ballot">@context.BallotNumberName</MudTd>
                <MudTd DataLabel="">
                    <MudTooltip Text="Edit">
                        <MudFab Color="MudBlazor.Color.Primary"                                
                                Icon="@Icons.Material.Outlined.ModeEditOutline"
                                OnClick="@(_ => Edit(context))" />
                    </MudTooltip> 
                    <MudTooltip Text="Delete">
                        <MudFab Color="MudBlazor.Color.Secondary"
                                Icon="@Icons.Material.Outlined.DeleteOutline"
                                OnClick="@(_ => context.IsDeleted = true)" />
                    </MudTooltip>  
                </MudTd>
            </RowTemplate>
        </MudTable>
    </Div>
</Div>

