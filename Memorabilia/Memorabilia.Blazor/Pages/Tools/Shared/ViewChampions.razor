@inherits ViewSportTools<ChampionModel>

<DivPad2>
    <DivRowCol12>
        <MudTable Bordered="false"
                  Dense="false"
                  Filter="new Func<ChampionModel,bool>(FilterFunc1)"
                  Hover="true"
                  Items="@Model.Champions.ToList()"
                  Striped="true">
            <ToolBarContent>
                <ToolbarTitle Text="@ChampionType.Name" />
                <MudSpacer />
                <ToolbarSearchField SearchText="@Search"
                                    SearchTextChanged="@((p) => Search = p)" />
            </ToolBarContent>
            <HeaderContent>
                <MudTh>
                    <MudTableSortLabel SortBy="new Func<ChampionModel, object>(x => x.TeamName)">Team</MudTableSortLabel>
                </MudTh>
                <MudTh>
                    <MudTableSortLabel SortBy="new Func<ChampionModel, object>(x => x.Franchise)">Franchise</MudTableSortLabel>
                </MudTh>
                <MudTh>
                    <MudTableSortLabel SortBy="new Func<ChampionModel, object>(x => x.Year)">Year</MudTableSortLabel>
                </MudTh>
            </HeaderContent>
            <RowTemplate>
                <MudTd DataLabel="Team">@context.TeamName</MudTd>
                <MudTd DataLabel="Franchise">@context.Franchise</MudTd>
                <MudTd DataLabel="Year">@context.Year</MudTd>
            </RowTemplate>
            <PagerContent>
                <MudTablePager />
            </PagerContent>
        </MudTable>
    </DivRowCol12>
</DivPad2>
