@inherits ImagePage

<DivPad2>
    <Content>
        <DivRowCol3>
            <Content>
                <AwardTypeAutoComplete ResetValueOnEmptyText="false"
                                       SelectedValue="_viewModel.AwardType"
                                       SelectionChanged="(awardType) => OnInputChange(awardType)"
                                       SportIds="_viewModel.SportIds" />
            </Content>
        </DivRowCol3>
        <DivRowCol6 Hidden="@(_viewModel.AwardType?.Id == null)">
            <Content>
                <MudTable Bordered="false"
                          Dense="false"
                          Filter="new Func<AwardViewModel,bool>(FilterFunc1)"
                          Hover="true"
                          Items="@_viewModel.PersonAwards.ToList()"
                          Striped="true">
                    <ToolBarContent>
                        <MudText Typo="Typo.h6">@_viewModel.AwardTypeName</MudText>
                        <MudSpacer />
                        <MudTextField @bind-Value="Search"
                                      Adornment="Adornment.Start"
                                      AdornmentIcon="@Icons.Material.Filled.Search"
                                      Class="mt-0"
                                      IconSize="MudBlazor.Size.Medium"
                                      Placeholder="Search" />
                    </ToolBarContent>
                    <HeaderContent>
                        <MudTh></MudTh>
                        <MudTh>Person</MudTh>
                        <MudTh>Year</MudTh>
                    </HeaderContent>
                    <RowTemplate>
                        <MudTd DataLabel="">
                            <MudTooltip>
                                <MudImage @onclick="@(_ => NavigationManager.NavigateTo(context.ProfileLink))" 
                                          Class="rounded-lg can-click"
                                          Elevation="5"
                                          Height="200"
                                          ObjectFit="@ObjectFit.Cover"
                                          Src="@(GetImageData(context.PersonImageFileName))"
                                          Width="200" />
                            </MudTooltip>
                        </MudTd>
                        <MudTd DataLabel="Person">
                            <MudLink Href="@context.ProfileLink">@context.PersonName</MudLink>
                        </MudTd>
                        <MudTd DataLabel="Year">@context.Year</MudTd>
                    </RowTemplate>
                    <PagerContent>
                        <MudTablePager />
                    </PagerContent>
                </MudTable>
            </Content>
        </DivRowCol6>
    </Content>
</DivPad2>
