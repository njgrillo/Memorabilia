@inherits SportProfile

<MudTable Bordered="false"
          Dense="false"
          Filter="new Func<SingleSeasonFranchiseRecordProfileModel, bool>(Filter)"
          Hover="true"
          Items="SingleSeasonFranchiseRecords"
          RowsPerPage="5"
          Striped="true">
    <ToolBarContent>
        <ToolbarTitle><b>Single Season Franchise Records</b></ToolbarTitle>
        <MudSpacer />
        <ToolbarSearchField SearchText="@Search"
                            SearchTextChanged="@((p) => Search = p)" />
    </ToolBarContent>
    <HeaderContent>
        <SortedColumn SortBy="new Func<SingleSeasonFranchiseRecordProfileModel, object>(x => x.FranchiseName)" Title="Franchise" />
        <SortedColumn SortBy="new Func<SingleSeasonFranchiseRecordProfileModel, object>(x => x.RecordTypeName)" Title="Record" />
        <SortedColumn SortBy="new Func<SingleSeasonFranchiseRecordProfileModel, object>(x => x.Record)" Title="" />
    </HeaderContent>
    <RowTemplate>
        <MudTd DataLabel="Franchise">@context.FranchiseName</MudTd>
        <MudTd DataLabel="Record">@context.RecordTypeName</MudTd>
        <MudTd>@context.Record</MudTd>
    </RowTemplate>
    <PagerContent>
        <MudTablePager />
    </PagerContent>
</MudTable>
