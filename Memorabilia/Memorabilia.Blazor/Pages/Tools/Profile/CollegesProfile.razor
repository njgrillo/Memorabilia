@inherits PersonProfile 

<MudTable Bordered="false"
          Dense="false"
          Hover="true"
          Items="Colleges"
          Striped="true">
    <ToolBarContent>
        <ToolbarTitle Text="Colleges" />
    </ToolBarContent>
    <HeaderContent>
        <MudTh>Name</MudTh>
        <MudTh>Begin Year</MudTh>
        <MudTh>End Year</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd DataLabel="Name">@(Constant.College.Find(context.CollegeId)?.Name ?? string.Empty)</MudTd>
        <MudTd DataLabel="Begin Year">@(context.BeginYear.HasValue ? context.BeginYear.Value : string.Empty)</MudTd>
        <MudTd DataLabel="End Year">@(context.EndYear.HasValue ? context.EndYear.Value : string.Empty)</MudTd>
    </RowTemplate>
</MudTable>
