<Dialog DisplayCancelButton="true"
        DisplayCloseButton="true"
        OnCancel="Cancel"
        OnClose="Cancel">
    <DialogContent>
        <MemorabiliaFilter InitialAcquistionTypeIds="AcquisitionTypeIds"
                           OnFilter="OnFilter" />
        <MemorabiliaDetailGrid CanSelect="true"
                               Filter="_filter"
                               GridLoaded="@((p) => _reloadGrid = false)"
                               MemorabiliaSelected="@((p) => SelectedMemorabilia = p)"
                               ReloadGrid="_reloadGrid"
                               ShowActions="false" />
    </DialogContent>
    <ActionsContent>
        <AddButton OnClick="Add" />
    </ActionsContent>
</Dialog>
