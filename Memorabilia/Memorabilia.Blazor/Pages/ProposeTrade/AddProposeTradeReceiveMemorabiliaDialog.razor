<Dialog DisplayCancelButton="true"
        DisplayCloseButton="true"
        OnCancel="Cancel"
        OnClose="Cancel">
    <DialogContent>
        <SiteMemorabiliaFilter OnFilter="OnFilter" />
        <SiteMemorabiliaDetailGrid CanSelect="true"
                                   Filter="_filter"
                                   MemorabiliaSelected="@((p) => SelectedMemorabilia = p)"
                                   ShowActions="false"
                                   UserId="UserId" />
    </DialogContent>
    <ActionsContent>
        <AddButton OnClick="Add" />
    </ActionsContent>
</Dialog>
