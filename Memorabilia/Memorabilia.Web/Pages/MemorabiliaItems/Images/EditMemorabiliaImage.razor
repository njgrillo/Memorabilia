@page "/Memorabilia/Image/Edit/{memorabiliaId:int}"

@inherits WebPage

<MemorabiliaImageEditor DomainImageRootPath="@DomainImageRootPath" 
                        MemorabiliaId="@MemorabiliaId"
                        UploadPath="@UploadPath"
                        UserId="@UserId" />
