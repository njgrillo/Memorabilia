@page "/Memorabilia/View/Gallery"

@inherits WebPage

<PageLoader>
    <Content>
        <ViewGallery DomainImageRootPath="@DomainImageRootPath" 
                     ImageRootPath="@($"{Path.Combine(MemorabiliaImageRootPath, UserId.ToString())}")"
                     UserId="@UserId" />
    </Content>
</PageLoader>
